{
  "name": "ipsumm",
  "type": "module",
  "version": "0.0.1",
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "pnpm build && npx wrangler deploy --config dist/server/wrangler.json",
    "db:migrate": "npx wrangler d1 execute ipsumm-db --remote --file=migrations/0001_auth_tables.sql"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.1",
    "@mlc-ai/web-llm": "^0.2.82",
    "astro": "^6.0.4",
    "idb": "^8.0.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "fake-indexeddb": "^6.2.5",
    "vitest": "^4.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  }
}